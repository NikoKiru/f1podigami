# Tooling used by CI (lint, format, coverage, dependency audit).
# Runtime deps live in requirements.txt.
-r requirements.txt
# Upper bound is deliberate: ruff 0.16 changed formatter output (incl. Markdown
# code blocks), and an unpinned CI ruff silently failed every PR's format gate —
# blocking the auto/update-data auto-merge (2026-07-25 Hungary quali stall).
# Bump the bound only together with a repo-wide `ruff format` in the same PR.
ruff>=0.16.5,<0.17
pytest-cov>=7.1.0
pip-audit>=2.10.1
