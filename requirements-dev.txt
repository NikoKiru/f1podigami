# Tooling used by CI (lint, format, coverage, dependency audit).
# Runtime deps live in requirements.txt.
-r requirements.txt
# Upper bound is deliberate: a ruff minor can change formatter output, and an
# unpinned CI ruff then silently fails every PR's format gate — blocking the
# auto/update-data auto-merge (2026-07-25 Hungary quali stall). Bump the bound
# only in a deliberate PR that carries whatever `ruff format` now wants.
# 0.16 added Markdown to ruff's default `include`; pyproject excludes `*.md`
# so docs/ snippets stay as written (#332).
ruff>=0.16.8,<0.17
pytest-cov>=7.1.0
pip-audit>=2.10.1
